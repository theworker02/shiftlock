apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: shiftlock-leases
rules:
  - apiGroups: ["coordination.k8s.io"]
    resources: ["leases"]
    verbs: ["get", "create", "update"]
---
# Bind this Role to your workload ServiceAccount.
# Use backend/kubernetes.LeaseClient wrapping client-go.
