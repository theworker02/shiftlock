# TUI

A full interactive TUI is **deferred**. Optional stub:

```bash
go run ./cmd/shiftlock tui
```

prints a note pointing operators to CLI + inspect tools. Isolating TUI dependencies keeps the core module lightweight.
