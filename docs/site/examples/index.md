# Examples

```bash
go run ./examples/singleton-worker
go run ./examples/runtime-supervisor
go run ./examples/leader-election
go run ./examples/emergency-lockdown
go run ./examples/maintenance-mode
go run ./examples/capability-authorization
go run ./examples/quorum-deployment
go run ./examples/secure-control-plane
```

`secure-control-plane` is the multi-step Phase 6 demo (ownership, signed config, capabilities, forged-cap lockdown, audit verify).
