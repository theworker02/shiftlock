# Quorum

Quorum is expressed via barrier `PolicyQuorum` (majority of MaxParticipants) and Runtime `Quorum()` helpers.

Duplicate participant IDs are rejected. Multi-node Byzantine quorum protocols remain out of scope for Phase 6.
